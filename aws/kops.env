export bucket_name="my-bucket-for-kops" # Use unique name
export region="us-west-2" # aws region
export zones="${region}b,${region}c"
export node_count=2
export node_size="t2.medium"
export KOPS_CLUSTER_NAME="kops.example.com" # if not using dns use k8.local, ie kops.k8s.local
export KOPS_STATE_STORE="s3://${bucket_name}" 
