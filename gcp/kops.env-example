export node_count=2
export bucket_name="bucket" # uniq bucket name
export region="us-west1"
export zones="${region}-b,${region}-c"
export KOPS_CLUSTER_NAME="kops.k8s.local" #if you want local dns use k8s.local, ie kops.k8s.local
export KOPS_STATE_STORE="gs://${bucket_name}"
export PROJECT="my-project"
export GOOGLE_APPLICATION_CREDENTIALS="${GOOGLE_APPLICATION_CREDENTIALS:-authfile.json}"
export KOPS_FEATURE_FLAGS="AlphaAllowGCE"
